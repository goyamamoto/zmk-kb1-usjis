# Kconfig for the project's Keychron B1 Pro US build.
# Read after app/boards/shields/keychron/b1/us/keychron_b1_us.conf in the
# pinned Keychron ZMK. Empty on purpose: the compatible build keeps the
# baseline's configuration. Options for the US-JIS feature are added with it.
